<Page x:Class="WpfApp.SavedBuildsPage"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
          xmlns:local="clr-namespace:WpfApp"
          mc:Ignorable="d" 
          d:DesignHeight="450" d:DesignWidth="800"
          Title="SavedBuildsPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Сохраненные сборки" FontSize="20" FontWeight="Bold" Margin="5"/>

        <ListView x:Name="BuildsList" Grid.Row="1" Margin="5" SelectionChanged="BuildsList_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" Width="200" DisplayMemberBinding="{Binding name}"/>
                    <GridViewColumn Header="Автор" Width="150" DisplayMemberBinding="{Binding author}"/>
                    <GridViewColumn Header="Количество деталей" Width="120" DisplayMemberBinding="{Binding PartsCount}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="5">
            <Button x:Name="LoadButton" Content="Загрузить сборку" Width="120" Margin="5" Click="LoadButton_Click"/>
        </StackPanel>
    </Grid>
</Page>