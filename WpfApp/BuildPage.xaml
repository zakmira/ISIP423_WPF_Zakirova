<Page x:Class="WpfApp.BuildPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="BuildPage">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Выбор комплектующих" FontSize="20" FontWeight="Bold" Margin="5"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
                <Button x:Name="BtnCPU" Content="Процессор" Width="100" Margin="2" Click="Category_Click"/>
                <Button x:Name="BtnGPU" Content="Видеокарта" Width="100" Margin="2" Click="Category_Click"/>
                <Button x:Name="BtnRAM" Content="Оперативная память" Width="130" Margin="2" Click="Category_Click"/>
                <Button x:Name="BtnMotherboard" Content="Материнская плата" Width="120" Margin="2" Click="Category_Click"/>
                <Button x:Name="BtnCase" Content="Корпус" Width="80" Margin="2" Click="Category_Click"/>
                <Button x:Name="BtnPSU" Content="Блок питания" Width="100" Margin="2" Click="Category_Click"/>
                <Button x:Name="BtnCooler" Content="Кулер" Width="80" Margin="2" Click="Category_Click"/>
                <Button x:Name="BtnStorage" Content="Накопитель" Width="100" Margin="2" Click="Category_Click"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5">
                <TextBox x:Name="SearchBox" Width="200" Margin="2" TextChanged="SearchBox_TextChanged"/>
                <ComboBox x:Name="ManufacturerFilter" Width="150" Margin="2" SelectionChanged="ManufacturerFilter_Changed"/>
                <TextBlock Text="Производители:" VerticalAlignment="Center" Margin="5,0"/>
            </StackPanel>

            <ListView x:Name="PartsList" Grid.Row="3" Margin="5" SelectionChanged="PartsList_SelectionChanged">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Название" Width="250" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Производитель" Width="120" DisplayMemberBinding="{Binding ManufacturerName}"/>
                        <GridViewColumn Header="Цена" Width="100" DisplayMemberBinding="{Binding Price,StringFormat={}{0:F0} ₽}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <Grid Grid.Column="1" Background="#F0F0F0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Выбранные комплектующие" FontSize="18" FontWeight="Bold" Margin="5"/>

            <ListView x:Name="SelectedPartsList" Grid.Row="1" Margin="5">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Тип" Width="100" DisplayMemberBinding="{Binding PartTypeName}"/>
                        <GridViewColumn Header="Название" Width="200" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="Цена" Width="80" DisplayMemberBinding="{Binding Price, StringFormat={}{0:F0} ₽}"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Grid.Row="2" Margin="5">
                <TextBlock x:Name="TotalPriceText" Text="Общая стоимость: 0 ₽" FontSize="16" FontWeight="Bold" Margin="5"/>
                <TextBlock x:Name="CompatibilityText" Text="Совместимость: OK" Foreground="Green" Margin="5"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Margin="5">
                <TextBox x:Name="AssemblyNameBox" Text="Моя сборка" Margin="5"/>
                <TextBox x:Name="AuthorNameBox" Text="Автор" Margin="5"/>
                <Button x:Name="SaveButton" Content="Сохранить сборку" Margin="5" Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>