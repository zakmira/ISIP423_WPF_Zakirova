<Page x:Class="WpfApp1.Pages.Page3"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" Background="BlanchedAlmond"
      Title="Page3">


    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
               Text="Оформление заказа"
               FontSize="28"
               FontWeight="Bold"
               HorizontalAlignment="Center"
               Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" MaxWidth="400">
                <TextBlock Text="ФИО:" Margin="0,0,0,5" FontSize="20"/>
                <TextBox x:Name="NameBox" Margin="0,0,0,15" Padding="5" Width="300"/>

                <TextBlock Text="Email:" Margin="0,0,0,5" FontSize="20"/>
                <TextBox x:Name="MailBox" Margin="0,0,0,15" Padding="5" Width="300"/>

                <TextBlock Text="Адрес доставки:" Margin="0,0,0,5" FontSize="20"/>
                <TextBox x:Name="AddressBox" Margin="0,0,0,15" Padding="5" Width="300"/>
            </StackPanel>

            <StackPanel Grid.Column="2" VerticalAlignment="Top">
                <TextBlock x:Name="TotalAmountText"
                       Text="Итого: 0 ₽"
                       FontSize="18"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="60,30,0,30"/>

                <Button x:Name="OrderButton"
                        Click="OrderButton_Click"
                        Content="Заказать"
                        HorizontalAlignment="Center"
                        Padding="30,15"
                        Background="Chocolate"
                        FontSize="16"
                        Margin="60,10,0,0"/>
                    
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="2" Grid.Column="1"
                  VerticalScrollBarVisibility="Auto"
                  MaxHeight="250"
                  Margin="0,10,0,0">
            <ItemsControl x:Name="OrderItemsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Image Grid.Column="0"
                               Source="{Binding ImagePath}"
                               Width="40" Height="40"
                               Stretch="Uniform"/>

                            <TextBlock Grid.Column="1"
                                   Text="{Binding Name}"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   Margin="10,0"/>

                            <TextBlock Grid.Column="2"
                                   Text="{Binding Price,StringFormat=C, ConverterCulture=ru-RU}"
                                   VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>